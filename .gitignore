d outputs
**/bin/
**/obj/

# Test and coverage artifacts
TestRcoverage-*/
*.cexml

# IDE and editor state
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates

# Logs and temp files
*.log
*.tmp
*.cache

# Local environment files
.env
.env.*
!.env.example

# SQLite data directory (bind-mounted in Docker / local dev)
data/*.db
data/*.db-shm
data/*.db-wal
data/*.db-journal

# Qdrant vector store runtime data (bind-mounted in Docker / local dev)
data/qdrant/

# Config overrides with secrets (use appsettings.Development.example.json as template)
appsettings.Development.json
!appsettings.Development.example.json

# Local databases
*.db
*.db-shm
*.db-wal

# Published build outputs
publish/
.claude/settings.local.json
.claude/settings.local.json
.claude/settings.local.json

# Docker image exports
docker-images/